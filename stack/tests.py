from django.test import TestCase

# Test that cards are randomly drawn

# Test that deleting a card results in a new card in its place

# Test that duplicate cards aren't allowed until reaching the end of the deck, at which point deck is shuffled (both)

# Test that you can't query the server and get more cards than the hand limit


